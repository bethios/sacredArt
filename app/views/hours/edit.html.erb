edit hours
<%= form_for @hour do |f| %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body, cols: "40", rows: "5"  %>
  </div>
  <div class="form-group">
    <%= f.submit "Save" %>
  </div>
<% end %>
